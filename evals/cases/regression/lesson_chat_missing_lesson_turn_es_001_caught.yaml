id: lesson_chat_missing_lesson_turn_es_001_caught
suite: regression
mode: lesson
skill: exercise_tutor
locale:
  native: en
  target: es
input:
  system_from_skills: true
  context_fixture: fixtures/learner_a2_travel_es.json
  user_message: "I go to store yesterday"
  history: []
checks:
  deterministic:
    - extract_lesson_turn
  judge:
    rubric: lesson_turn_v1
notes: >-
  Known-bad: Spanish prose with no json:lesson_turn fence. extract_lesson_turn
  must return None. expect_fail: true (expect_outcome=fail) so dropping the
  check fails CI.
expect_fail: true
