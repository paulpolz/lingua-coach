apiVersion: 1

providers:
  - name: lingua-coach
    orgId: 1
    folder: Lingua Coach
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
